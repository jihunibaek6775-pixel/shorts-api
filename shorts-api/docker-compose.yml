version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: shorts-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shorts-api
    ports:
      - "8000:8000"
    volumes:
      # 개발 중 코드 변경 반영 (프로덕션에서는 제거 가능)
      - ./app:/app/app
    env_file:
      - .env
    environment:
      # .env 파일의 값을 오버라이드하거나 추가
      - DATABASE_URL=postgresql://postgres:postgres123@db:5432/shortsapi
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 120s
      timeout: 15s
      retries: 3
      start_period: 60s

  # Nginx 프록시 (나중에)
  # nginx:
  #   image: nginx:alpine
  #   container_name: shorts-nginx
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - api
  #   restart: unless-stopped

volumes:
  postgres_data:  # PostgreSQL 데이터 영구 저장